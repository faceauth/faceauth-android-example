<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="live.faceauth.example.DashboardActivity">

  <LinearLayout android:layout_width="match_parent"
      android:orientation="vertical"
      android:padding="4dp"
      android:layout_height="wrap_content">

    <include layout="@layout/welcome_card" />

    <include
        android:id="@+id/register_card"
        layout="@layout/register_card"
        android:visibility="gone" />

    <include
        android:id="@+id/registered_card"
        layout="@layout/registered_card"
        android:visibility="gone" />

    <include layout="@layout/balance_card" />

    <include layout="@layout/actions_card" />


  </LinearLayout>

</ScrollView>
